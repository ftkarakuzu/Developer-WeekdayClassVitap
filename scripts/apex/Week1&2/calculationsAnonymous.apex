ArithmeticCalculations ac = new ArithmeticCalculations();

integer add = ac.addition(1,3);
system.debug('The sum of x and y is ' + add);

integer sub = ac.subtraction(1,3);
system.debug('The difference of x and y is ' + sub);

integer pro = ac.multiplication(1,3,5);
system.debug('The product of x, y, and m is ' + pro);